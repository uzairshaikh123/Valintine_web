import React from 'react';
import './failure.css'; // Import CSS file for styling (create this file if it doesn't exist)

const Failure = () => {
  return (
    <>
      
    </>
  );
};

export default Failure;
